import 'package:flutter/material.dart';

class TopTvPage extends StatelessWidget {
  const TopTvPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('top tv page'.toUpperCase()),
    );
  }
}