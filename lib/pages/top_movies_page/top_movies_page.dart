import 'package:flutter/material.dart';

class TopMoviesPage extends StatelessWidget {
  const TopMoviesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('top movies page'.toUpperCase()),
    );
  }
}